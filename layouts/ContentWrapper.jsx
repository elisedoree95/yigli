import React from 'react'

const ContentWrapper = ({ children }) => {
    return (
        <div>
            Content Wrapper
            {children}
        </div>
    )
}

export default ContentWrapper